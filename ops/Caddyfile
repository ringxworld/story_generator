{
  # TODO(#1): replace with production email once domain is final.
  email admin@example.com
}

story.example.com {
  encode gzip

  handle_path /api/* {
    reverse_proxy app:8000
  }

  handle {
    respond "story_gen service online" 200
  }
}
