# story_gen web studio

React + TypeScript front-end for authenticated story blueprint editing.

## Commands

- `npm install`
- `npm run dev`
- `npm run typecheck`
- `npm run test`
- `npm run build`

Set API base URL with `VITE_API_BASE_URL` if not using `http://127.0.0.1:8000`.
